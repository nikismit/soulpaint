using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleScaler : MonoBehaviour
{

   // [SerializeField] private List<GameObject> ParticleChildren;
    //[SerializeField] private ParticleSystem[] _Particle;
    //[SerializeField] private float Offset = .8f;

    //void Start()
    //{
    //    ScaleParticles(Offset);
    //}
    

    //public void ScaleParticles(float offset)
    //{
    //    _Particle = GetComponentsInChildren<ParticleSystem>();
    //     for (int i = 0; i < _Particle.Length; i++)
    //    {
    //        _Particle[i].gameObject.transform.localScale = new Vector3(offset * transform.localScale.x, offset * transform.localScale.y, offset * transform.localScale.z);
    //    }
    //}

}
